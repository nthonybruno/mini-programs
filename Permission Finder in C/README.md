# A Linux Permission Finder Written in C

Finds files with a specified set of permissions.

    Usage: ./pfind -d <directory> -p <permissions string> [-h]

Example:

     ./pfind -d test_dir -p rw-r--r--
